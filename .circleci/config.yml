version: 2.1
orbs:
  cypress: cypress-io/cypress@1
workflows:
  build:
    jobs:
      - cypress/run:
          install-command: npm ci
        run:
          environment:
          CYPRESS_credentials: ${{credentials}}
          CYPRESS_gmail_token: ${{gmail_token}}
       